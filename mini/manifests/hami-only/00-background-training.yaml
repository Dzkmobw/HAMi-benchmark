apiVersion: batch/v1
kind: Job
metadata:
  name: mini-background-training
  labels:
    benchmark.hami.io/plan: mini
    benchmark.hami.io/mode: hami-only
    benchmark.hami.io/workload: background-training
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        benchmark.hami.io/plan: mini
        benchmark.hami.io/mode: hami-only
        benchmark.hami.io/workload: background-training
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime
          imagePullPolicy: IfNotPresent
          command: ["python", "/workloads/mini_workload.py"]
          env:
            - name: WORKLOAD_KIND
              value: background-training
            - name: WORKLOAD_RUNTIME_SECONDS
              value: "150"
            - name: WORKLOAD_RESERVE_MIB
              value: "2200"
            - name: WORKLOAD_BATCH_SIZE
              value: "64"
          resources:
            limits:
              nvidia.com/gpu: "1"
              nvidia.com/gpumem: "3000"
              nvidia.com/gpucores: "30"
          volumeMounts:
            - name: workloads
              mountPath: /workloads
      volumes:
        - name: workloads
          configMap:
            name: hami-benchmark-mini-workloads
---
